# badges example

A demo app for the [badges](https://pub.dev/packages/badges) package. It shows badges on
app bar icons, tabs, the bottom navigation bar and avatars, plus the Twitter and Instagram
shapes, gradients and the slide animation on the cart badge (tap + / - to change it).

Run it with `flutter run` from this `example/` directory (Android, iOS or Windows).
